'use strict';

/**
 * neighborhood-page-slug router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::neighborhood-page-slug.neighborhood-page-slug');
