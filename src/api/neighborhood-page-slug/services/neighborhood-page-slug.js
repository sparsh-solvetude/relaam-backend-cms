'use strict';

/**
 * neighborhood-page-slug service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::neighborhood-page-slug.neighborhood-page-slug');
