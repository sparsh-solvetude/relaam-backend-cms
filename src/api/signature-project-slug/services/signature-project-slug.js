'use strict';

/**
 * signature-project-slug service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::signature-project-slug.signature-project-slug');
