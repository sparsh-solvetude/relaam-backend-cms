'use strict';

/**
 * signature-project-slug router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::signature-project-slug.signature-project-slug');
